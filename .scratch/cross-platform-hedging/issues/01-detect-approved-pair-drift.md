# Detect approved product-pair drift

Status: needs-triage
Type: task

Periodically inspect the broker-provided specifications of approved product pairs. When an approved pairing differs from its reviewed specification, disable it until a human resolves the difference and explicitly re-enables it.
